# === Arbordex Environment Configuration ===
# Copy this file to .env and update values as needed
# Note: All variables have sensible defaults if not provided

# --- Server Configuration ---

# Environment mode: development, staging, or production
# Default: development
# - development:  Source code, no HTTPS, hot reload
# - staging:      Compiled code, HTTPS enforcement, full security headers
# - production:   Same as staging (use for actual deployments)
NODE_ENV=development

# Server port (0-65535)
# Default: 3000
PORT=3000

# Server host/bind address
# Default: localhost
HOST=localhost

# --- Testing Configuration ---

# Base URL for API tests
# Default: http://localhost:3000
# Use to test against different environments:
#   TEST_BASE_URL=http://localhost:4000 npm test
#   TEST_BASE_URL=https://staging.example.com npm test
TEST_BASE_URL=http://localhost:3000
